- name: shell vs command
  hosts: local
  connection: local
  tasks:
  - name: execute ls command
    ansible.builtin.command : ls
    register: resul

  - name: print result
    ansible.builtin.debug:
      msg: "{{resul}}"  